// TerraformTaskV5 is the one task whose target genuinely differs from the
// other 10 (ES2020 vs ES6, issue #409) -- kept here as an explicit, documented
// per-task override rather than unexplained drift.
{
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
        "target": "ES2020"
    },
    "include": [
        "src/**/*"
    ]
}